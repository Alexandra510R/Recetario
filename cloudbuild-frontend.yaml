steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-f', 'Dockerfile', '-t', 'us-central1-docker.pkg.dev/$PROJECT_ID/recetario-repo/frontend:latest', '.']
images:
- 'us-central1-docker.pkg.dev/$PROJECT_ID/recetario-repo/frontend:latest'
